# Prefab fixtures

This bounded workspace root holds authored prefab registry and placement fixtures
used by public ASHA compatibility and Studio integration proofs. Runtime authority
still validates any fixture before bootstrap.
